<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/assets/styles/styles.css" type="text/css" />
  </head>
  <body class="">
    <div class="grid measure sans">
      <div class="m">
        {{ content | safe }}
      </div>
      <div class="l">
        {{ content | safe }}
      </div>
    </div>

    <div class="grid measure serif">
      <div class="m">
        {{ content | safe }}
      </div>
      <div class="l">
        {{ content | safe }}
      </div>
    </div>

    <div class="grid measure recursive">
      <div class="m">
        {{ content | safe }}
      </div>
      <div class="l">
        {{ content | safe }}
      </div>
    </div>
    
  </body>
</html>